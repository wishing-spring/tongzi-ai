# 童子 · 极简群域架构（终版）

**日期**: 2026-05-20 | **状态**: 理论定版 | **替代**: 赤子之心/内丹/十二锚全部旧理论

---

## 一、本质

童子不是一个算法、不是一个模型、不是一个处理器。

是一个 **F₂ⁿ 空间**。卦是空间里的点。卦与卦之间的关联、匹配、排布、运动——是空间本身的属性，不是外部规则。

```
不是: 输入→编码→处理→输出（流水线）
而是: F₂ⁿ 空间 + 卦群 + 自然交互法则
```

---

## 二、四条公理（不可动，万维通用）

| # | 公理 | 含义 |
|:--|:--|:--|
| 1 | a⊕b = b⊕a | XOR 交换 |
| 2 | a⊕a = 0 | XOR 自消 |
| 3 | d(a,b) = popcount(a⊕b) | 汉明距离 |
| 4 | d(Rₖ(a),Rₖ(b)) = d(a,b) | 位旋转保距 |

四条不含 n、不含维度、不含任何外部参数。16 爻成立，65536 爻也成立。

衍生基础算子：AND（a∧b ∈ F₂ⁿ）、位循环旋转 Rₖ(v)、汉明球 S_r(c)。

---

## 三、φ 母体——卦的出生与身份

φ = 1.6180339... 的二进制小数展开是卦的唯一来源。

```
φ_bits = 1001111000110111...

卦 = φ_bits[pos:pos+length]

每个卦天生携带:
  pos    → 出生序号 id_t（第几个从 φ 取出）
  length → 层归属 id_l（取了多少位，2爻/3爻/...）
  滑动1位 → 前驱/后继亲缘
```

**{id_t, id_l} = 卦的身份证。** 全域唯一。不需存储——从 φ 位置即可推导。

---

## 四、八条核心运算

### 1. 碰撞（信息完全展开）
```
collide(a, b) → (a⊕b, a∧b)
```
输出差异向量和共识向量。两入两出，信息不丢失。

### 2. 绕心盘旋
```
orbit(v, c, k) = Rₖ(v⊕c) ⊕ c
```
v 绕中心 c 在等距球面上旋转。保持 d(v,c) 不变。

### 3. 径向伸缩
```
stretch(v, c, λ) = v ⊕ λ·(v⊕c)
λ∈{0,1}: 0=原位, 1=推至对面
```
改变 v 与中心 c 的距离。λ 逐位异或。

### 4. 格雷巡检环
```
gray(n) = n ⊕ (n>>1)
```
全卦排成环，相邻只差 1 位。天然巡检路径。换维度自动适配。

### 5. 精确距离
```
radius(v, c) = d(v, c)
```

### 6. 批量聚类
```
Ω(c, r) = {x | d(x, c) ≤ r}
```
以 c 为中心、半径 r 的汉明球内全部卦。

### 7. 核心度
```
centrality(v, c) = -d(v, c)
```

### 8. 分层索引
```
layer(v) = id_l(v)          # 从 φ 取位长度
order(v) = id_t(v)          # 在 φ 里的出生序号
prev(v) = φ_bits[pos-1:pos-1+length]   # 前驱
next(v) = φ_bits[pos+1:pos+1+length]   # 后继
```

---

## 五、内生频控（非外部定时）

### 顺位频控
```
f₁(x) = f₀ / (1 + id_t(x))
```
生得早（id_t 小）→ 频次高。生得晚 → 频次低。主干高频，末梢低频。

### 层差频控
```
f₂(x) = f₀ >> id_l(x)
```
低层密交互，高层稀交互。位移即除法，无浮点。

### 运动联动
```
步长 k 大 → orbit 转得快 → 碰面多
|λ| 大 → stretch 幅度大 → 跨域碰撞多
```

---

## 六、位分区

16 位分四区：

| 位域 | 用途 |
|:--|:--|
| 位 15-12 | 状态码（基底/交互/排布/记忆） |
| 位 11-8 | 参数区（k, λ, mask） |
| 位 7-4 | 身份区（id_l, id_t 压缩） |
| 位 3-0 | 数据区（卦的核心内容） |

状态码 = 自指——卦知道自己处于哪个状态。维度无关——换 n 只需调整各区宽度比例。

---

## 七、密度触发自清

```
if local_density > threshold:
    merge(nearest_pair)  # 合并最近的两个卦
```
不是外部定时清——是空间自己太挤了自动合并。density 用汉明球 r=1 内的卦数计。

---

## 八、砍掉的旧部件

| 旧部件 | 砍掉原因 |
|:--|:--|
| Loom 编织 (tongzi_loom.py) | 碰撞+绕心+伸缩覆盖了编织的功能 |
| Balancer 阴阳判定 | 阈值绑定 n；判定由 f₁+f₂ 内生替代 |
| Responder 9 句固定应答 | 有形无魂；输出即碰撞结果 |
| 12 锚定局 | 12 是固定数 |
| ⌊√n⌋ / ⌊n/3⌋+4 | 含 n |
| 外部定时浇花 | 密度自清 + 内生频控替代 |
| 投影映射 | 虚饰，无实质帮助 |

---

## 九、框架特性

| 特性 | 实现 |
|:--|:--|
| **万维通用** | 四条公理 + 八运算不含 n |
| **自带秩序** | φ 出生序号 = 天然顺序 |
| **自指元信息** | 状态码位 = 卦知道自己的类型 |
| **大小卦共存** | 不同 length 的卦同空间独立存在 |
| **天然层级** | id_l 区分内外圈层 |
| **点对点精准** | 距离+身份双频控 |
| **形态自由** | 出生序排、格雷环排、汉明圈层排 |

---

## 十、已知代价（接受）

| 代价 | 说明 |
|:--|:--|
| 无连续过渡 | F₂ 离散，无梯度 |
| 输出不可直接阅读 | 碰撞结果是位串，非人类语言 |
| 完全可逆 | 无天然遗忘机制（待补不可逆操作） |
| 演化困在组合池 | 有限集所有操作皆重组 |
| 伪立体 | 汉明球非真几何体 |
| 无外部时钟 | 无人投喂时沉寂（=设计） |

---

## 十一、代码对应

源码保留：

| 文件 | 保留内容 |
|:--|:--|
| `tongzi_constants.py` | VEC_DIM=16, FULL_MASK |
| `tongzi_core.py` | BitStore + φ编码(带pos) + collider + orbit + stretch |
| `tongzi.py` | 交互入口 |

砍掉：

| 文件 | 状态 |
|:--|:--|
| `tongzi_loom.py` | 删除 |
| `tongzi_mao.py` | 删除（Balancer） |
| `tongzi_data.py` | 删除（Responder 固定应答） |
| `tongzi_seeds.py` | 删除（固定种子） |
| `water.py` | 删除（外部定时） |
| `boundary_test.py` | 删除（Loom测试） |

---

**理论终版。不再追加新概念。下一阶段：代码落地。**
